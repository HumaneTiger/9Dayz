// @ts-check
/**
 * @import { CardDeck, Card } from '../../data/definitions/cards-definitions.js'
 */

// import { CardsDefinitions } from '../../data/index.js';

export default {
  /*
  createBattleDeck: function () {
    const battleDeck = [];
    return battleDeck;
  },

  createBattleDeckWithoutType: function (type) {
    return {
      {
      battleDeck: battleDeck,
        numberOfTools: numberOfTools,
      }
    }
  },

  createOpponentDeck: function () {
  },

  getBattleDeckProps: function () {
    const battleDeckProps = CardsDefinitions[type];
    return battleDeckProps;
  }
  */
};
